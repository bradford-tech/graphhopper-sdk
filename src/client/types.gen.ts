/* eslint-disable */
// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
  baseUrl: "https://graphhopper.com/api/1" | (string & {});
};

export type ClusterResponse = {
  copyrights?: Array<string>;
  /**
   * Indicates the current status of the job
   */
  status?: "waiting_in_queue" | "processing" | "finished";
  waiting_time_in_queue?: number;
  processing_time?: number;
  clusters?: Array<Cluster>;
};

export type Cluster = {
  /**
   * Cluster size
   */
  quantity?: number;
  /**
   * Array of customer ids assigned to this specific cluster
   */
  ids?: Array<string>;
};

export type ClusterRequest = {
  configuration?: ClusterConfiguration;
  clusters?: Array<Clusters>;
  customers?: Array<ClusterCustomer>;
};

export type ClusterConfiguration = {
  /**
   * Specifies the response format. You can either choose `geojson` or `json`.
   */
  response_type?: string;
  routing?: ClusterConfigurationRouting;
  clustering?: ClusterConfigurationClustering;
};

export type Clusters = {
  /**
   * id of customer
   */
  name?: string;
  center?: ClusterCustomerAddress;
  /**
   * Specifies min. quantity of this cluster
   */
  min_quantity?: number;
  /**
   * Specifies max. quantity of this cluster
   */
  max_quantity?: number;
};

export type ClusterCustomer = {
  /**
   * id of customer
   */
  id?: string;
  address?: ClusterCustomerAddress;
  /**
   * demand of customer
   */
  quantity?: number;
};

export type ClusterCustomerAddress = {
  /**
   * Longitude
   */
  lon?: number;
  /**
   * Latitude
   */
  lat?: number;
  /**
   * Optional parameter. Provide a hint that includes only the street name for each address to better snap the coordinates (lon,lat) to road network. E.g. if there is an address or house with two or more neighboring streets you can control for which street the closest location is looked up.
   */
  street_hint?: string;
};

export type ClusterConfigurationRouting = {
  profile?: VehicleProfileId;
  /**
   * Cost per second (travel time)
   */
  cost_per_second?: number;
  /**
   * Cost per meter (travel distance)
   */
  cost_per_meter?: number;
};

export type ClusterConfigurationClustering = {
  /**
   * Specifies the number of clusters
   */
  num_clusters?: number;
  /**
   * Specifies max. quantity in a cluster
   */
  max_quantity?: number;
  /**
   * Specifies min. quantity in a cluster
   */
  min_quantity?: number;
};

export type GeocodingResponse = {
  hits?: Array<GeocodingLocation>;
  /**
   * in ms
   */
  took?: number;
};

export type GeocodingLocation = {
  point?: GeocodingPoint;
  /**
   * The OSM ID of the entity
   */
  osm_id?: number;
  /**
   * N = node, R = relation, W = way
   */
  osm_type?: string;
  /**
   * The OSM key of the entity
   */
  osm_key?: string;
  /**
   * The name of the entity. Can be a boundary, POI, address, etc
   */
  name?: string;
  /**
   * The country of the address
   */
  country?: string;
  /**
   * The city of the address
   */
  city?: string;
  /**
   * The state of the address
   */
  state?: string;
  /**
   * The street of the address
   */
  street?: string;
  /**
   * The housenumber of the address
   */
  housenumber?: string;
  /**
   * The postcode of the address
   */
  postcode?: string;
};

export type GeocodingPoint = {
  /**
   * Latitude
   */
  lat?: number;
  /**
   * Longitude
   */
  lng?: number;
};

export type MatrixRequest = {
  profile?: VehicleProfileId;
  /**
   * The origin points for the routes in an array of `[longitude,latitude]`.
   */
  from_points?: Array<Array<number>>;
  /**
   * The destination points for the routes in an array of `[longitude,latitude]`.
   */
  to_points?: Array<Array<number>>;
  /**
   * See `point_hints`of symmetrical matrix
   */
  from_point_hints?: Array<string>;
  /**
   * See `point_hints`of symmetrical matrix
   */
  to_point_hints?: Array<string>;
  /**
   * See `snap_preventions` of symmetrical matrix
   */
  snap_preventions?: Array<string>;
  /**
   * See `curbsides`of symmetrical matrix
   */
  from_curbsides?: Array<string>;
  /**
   * See `curbsides`of symmetrical matrix
   */
  to_curbsides?: Array<string>;
  /**
   * Specifies which matrices should be included in the response. Specify one or more of the following options `weights`, `times`, `distances`. The units of the entries of `distances` are meters, of `times` are seconds and of `weights` is arbitrary and it can differ for different vehicles or versions of this API.
   */
  out_arrays?: Array<string>;
  /**
   * Specifies whether or not the matrix calculation should return with an error as soon as possible in case some points cannot be found or some points are not connected. If set to `false` the time/weight/distance matrix will be calculated for all valid points and contain the `null` value for all entries that could not be calculated. The `hint` field of the response will also contain additional information about what went wrong (see its documentation).
   */
  fail_fast?: boolean;
};

export type SymmetricalMatrixRequest = {
  profile?: VehicleProfileId;
  /**
   * Specify multiple points for which the weight-, route-, time- or distance-matrix should be calculated as follows: `[longitude,latitude]`. In this case the origins are identical to the destinations. Thus, if there are N points, NxN entries are calculated. The order of the point parameter is important. Specify at least three points. Cannot be used together with `from_point` or `to_point.`.
   */
  points?: Array<Array<number>>;
  /**
   * Optional parameter. Specifies a hint for each point in the `points` array to prefer a certain street for the closest location lookup. E.g. if there is an address or house with two or more neighboring streets you can control for which street the closest location is looked up.
   */
  point_hints?: Array<string>;
  /**
   * Optional parameter. 'Snapping' is the process of finding the closest road location for GPS coordinates provided in the `points` array. The `snap_preventions` array allows you to prevent snapping to specific types of roads. For example, if the array includes `bridge`, then the routing engine will avoid snapping to a bridge, even if it is the closest road for the given point. Note that once snapped the routing algorithm can still route over bridges (or the other values). To avoid this you need to use the `custom_model`.
   */
  snap_preventions?: Array<
    "motorway" | "trunk" | "bridge" | "ford" | "tunnel" | "ferry"
  >;
  /**
   * Optional parameter. It specifies on which side a point should be relative to the driver when she leaves/arrives at a start/target/via point. You need to specify this parameter for either none or all points. Only supported for motor vehicles and OpenStreetMap.
   */
  curbsides?: Array<"right" | "left" | "any">;
  /**
   * Specifies which matrices should be included in the response. Specify one or more of the following options `weights`, `times`, `distances`. The units of the entries of `distances` are meters, of `times` are seconds and of `weights` is arbitrary and it can differ for different vehicle profiles or versions of this API.
   */
  out_arrays?: Array<string>;
  /**
   * Specifies whether or not the matrix calculation should return with an error as soon as possible in case some points cannot be found or some points are not connected. If set to `false` the time/weight/distance matrix will be calculated for all valid points and contain the `null` value for all entries that could not be calculated. The `hint` field of the response will also contain additional information about what went wrong (see its documentation).
   */
  fail_fast?: boolean;
};

export type MatrixResponse = {
  /**
   * The distance matrix for the specified points in the same order as the time matrix. The distances are in meters. If `fail_fast=false` the matrix will contain `null` for connections that could not be found.
   */
  distances?: Array<Array<number>>;
  /**
   * The time matrix for the specified points in the order [[from1->to1, from1->to2, ...], [from2->to1, from2->to2, ...], ...]. The times are in seconds. If `fail_fast=false` the matrix will contain `null` for connections that could not be found.
   */
  times?: Array<Array<number>>;
  /**
   * The weight matrix for the specified points in the same order as the time matrix. The weights for different vehicle profiles can have a different unit but the weights array is perfectly suited as input for Vehicle Routing Problems as it is currently faster to calculate. If `fail_fast=false` the matrix will contain `null` for connections that could not be found.
   */
  weights?: Array<Array<number>>;
  info?: ResponseInfo;
  /**
   * Optional. Additional response data.
   */
  hints?: Array<{
    /**
     * Short description of this hint
     */
    message?: string;
    /**
     * Details of this hint
     */
    details?: string;
    /**
     * Optional. An array of from_point indices of points that could not be found. Will only be added if `fail_fast=false` and some `from_point`s were not found.`
     */
    invalid_from_points?: Array<number>;
    /**
     * Optional. An array of to_point indices of points that could not be found. Will only be added if `fail_fast=false` and some `to_point`s were not found.`
     */
    invalid_to_points?: Array<number>;
    /**
     * Optional. An array of two-element arrays representing the from/to_point indices of points for which no connection could be found. Will only be added if `fail_fast=false` and some connections were not found.
     */
    point_pairs?: Array<Array<number>>;
  }>;
};

export type RouteRequest = {
  profile?: VehicleProfileId;
  /**
   * The query points used to calculate the route, requiring at least a start and a destination point, with optional via-points in between.
   *
   */
  points?: Array<Array<number>>;
  /**
   * Optional parameter. Specifies a hint for each point in the `points` array to prefer a certain street for the closest location lookup. E.g. if there is an address or house with two or more neighboring streets you can control for which street the closest location is looked up. Make sure you do not include the house number of city name and only the street name to improve the quality of the matching.
   */
  point_hints?: Array<string>;
  /**
   * Optional parameter. 'Snapping' is the process of finding the closest road location for GPS coordinates provided in the `points` array. The `snap_preventions` array allows you to prevent snapping to specific types of roads. For example, if the array includes `bridge`, then the routing engine will avoid snapping to a bridge, even if it is the closest road for the given point. Note that once snapped the routing algorithm can still route over bridges (or the other values). To avoid this you need to use the `custom_model`.
   */
  snap_preventions?: Array<
    "motorway" | "trunk" | "bridge" | "ford" | "tunnel" | "ferry"
  >;
  /**
   * Optional parameter. It specifies on which side a point should be relative to the driver when she leaves/arrives at a start/target/via point. You need to specify this parameter for either none or all points. Only supported for motor vehicle profiles and OpenStreetMap.
   */
  curbsides?: Array<"any" | "right" | "left">;
  /**
   * The locale of the resulting turn instructions. E.g. `pt_PT` for Portuguese or `de` for German.
   *
   */
  locale?: string;
  /**
   * If `true`, a third coordinate, the altitude, is included with all positions in the response.
   * This changes the format of the `points` and `snapped_waypoints` fields of the response, in both their
   * encodings. Unless you switch off the `points_encoded` parameter, you need special code on the
   * client side that can handle three-dimensional coordinates.
   *
   */
  elevation?: boolean;
  /**
   * Optional parameter. The following path details are available: `street_name`, `street_ref`, `street_destination`, `leg_time`, `leg_distance`, `roundabout`, `country`, `time`, `distance`, `max_speed`,
   * `max_weight`, `max_width`, `toll`, `road_class`, `road_class_link`, `road_access`, `road_environment`, `hazmat`, `hazmat_tunnel`, `hazmat_water`,
   * `lanes`, `surface`, `smoothness`, `hike_rating`, `mtb_rating`, `foot_network`, `bike_network`. Read more about the usage of path details [here](https://discuss.graphhopper.com/t/2539).
   *
   */
  details?: Array<string>;
  /**
   * Normally, the calculated route will visit the points in the order you specified them.
   * If you have more than two points, you can set this parameter to `"true"` and the points may be re-ordered to minimize the total travel time.
   * Keep in mind that the limits on the number of locations of the Route Optimization API applies, and the request costs more credits.
   *
   */
  optimize?: string;
  /**
   * If instructions should be calculated and returned
   *
   */
  instructions?: boolean;
  /**
   * If the points for the route should be calculated at all.
   *
   */
  calc_points?: boolean;
  /**
   * If `true`, the output will be formatted.
   *
   */
  debug?: boolean;
  /**
   * Allows changing the encoding of location data in the response. The default is polyline encoding, which is compact
   * but requires special client code to unpack. (We provide it in our JavaScript client library!)
   * Set this parameter to `false` to switch the encoding to simple coordinate pairs like `[lon,lat]`, or `[lon,lat,elevation]`.
   * See the description of the response format for more information.
   *
   */
  points_encoded?: boolean;
  /**
   * Use `true` to enable the flexible mode to use the `custom_model` or any of the following options below.
   *
   */
  "ch.disable"?: boolean;
  custom_model?: CustomModel;
  /**
   * Favour a heading direction for a certain point. Specify either one heading for the start point or as many as there are points.
   * In this case headings are associated by their order to the specific points. Headings are given as north based clockwise angle between 0 and 360 degree.
   * This headings parameter also influences the tour generated with `algorithm=round_trip` and forces the initial direction.  Requires `ch.disable=true`.
   *
   */
  headings?: Array<number>;
  /**
   * Time penalty in seconds for not obeying a specified heading. Requires `ch.disable=true`.
   *
   */
  heading_penalty?: number;
  /**
   * If `true`, u-turns are avoided at via-points with regard to the `heading_penalty`. Requires `ch.disable=true`.
   *
   */
  pass_through?: boolean;
  /**
   * Rather than looking for the shortest or fastest path, this parameter lets you solve two different problems related to routing:
   * With `alternative_route`, we give you not one but several routes that are close to optimal, but
   * not too similar to each other.
   * With `round_trip`, the route will get you back to where you started. This is meant for fun (think of
   * a bike trip), so we will add some randomness.
   * You can control both of these features with additional parameters, see below.
   *
   */
  algorithm?: "round_trip" | "alternative_route";
  /**
   * If `algorithm=round_trip`, this parameter configures approximative length of the resulting round trip.
   *
   */
  "round_trip.distance"?: number;
  /**
   * If `algorithm=round_trip`, this sets the random seed. Change this to get a different tour for each value.
   *
   */
  "round_trip.seed"?: number;
  /**
   * If `algorithm=alternative_route`, this parameter sets the number of maximum paths which should be calculated. Increasing can lead to worse alternatives.
   *
   */
  "alternative_route.max_paths"?: number;
  /**
   * If `algorithm=alternative_route`, this parameter sets the factor by which the alternatives routes can be longer than the optimal route. Increasing can lead to worse alternatives.
   *
   */
  "alternative_route.max_weight_factor"?: number;
  /**
   * If `algorithm=alternative_route`, this parameter specifies how similar an alternative route can be to the optimal route. Increasing can lead to worse alternatives.
   *
   */
  "alternative_route.max_share_factor"?: number;
};

/**
 * The custom_model modifies the routing behaviour of the specified profile. See the [detailed documentation](#tag/Custom-Model).
 * Below is a complete request example in Berlin that limits all speeds to 100km/h, excludes motorways and makes shorter routes a bit more likely than the default due to a higher distance_influence.
 * Note that it also includes the `"ch.disable": true` parameter which is required to make use of `custom_model`.
 *
 * ```json
 * {
 * "points": [
 * [
 * 13.31543,
 * 52.509535
 * ],
 * [
 * 13.29779,
 * 52.512434
 * ]
 * ],
 * "profile": "car",
 * "ch.disable": true,
 * "custom_model": {
 * "speed": [
 * {
 * "if": "true",
 * "limit_to": "100"
 * }
 * ],
 * "priority": [
 * {
 * "if": "road_class == MOTORWAY",
 * "multiply_by": "0"
 * }
 * ],
 * "distance_influence": 100
 * }
 * }
 * ```
 *
 */
export type CustomModel = {
  /**
   * See [speed customization](#tag/Custom-Model/Customizing-speed)
   */
  speed?: Array<{
    [key: string]: unknown;
  }>;
  /**
   * See [priority customization](#tag/Custom-Model/Customizing-priority)
   */
  priority?: Array<{
    [key: string]: unknown;
  }>;
  /**
   * Use higher values to prefer shorter routes. See [here](#tag/Custom-Model/Customizing-distance_influence) for more details.
   */
  distance_influence?: number;
  /**
   * Areas are given in a GeoJson format as FeatureCollection. See more details and an example [here](#tag/Custom-Model/Define-areas).
   *
   */
  areas?: FeatureCollection;
};

/**
 * The custom_model modifies the routing behaviour of the given `profile`. See the [detailed documentation](#tag/Custom-Model).
 * Here is a complete example that limits all speeds to 100km/h, avoids motorways and makes shorter routes a bit more likely than the default due to a higher distance_influence.
 *
 * ```json
 * {
 * "speed": [
 * {
 * "if": "true",
 * "limit_to": 100
 * }
 * ],
 * "priority": [
 * {
 * "if": "road_class == MOTORWAY",
 * "multiply_by": 0.1
 * }
 * ],
 * "distance_influence": 100
 * }
 * ```
 *
 */
export type CustomModelForProfile = {
  /**
   * See [speed customization](#tag/Custom-Model/Customizing-speed)
   */
  speed?: Array<{
    [key: string]: unknown;
  }>;
  /**
   * See [priority customization](#tag/Custom-Model/Customizing-priority)
   */
  priority?: Array<{
    [key: string]: unknown;
  }>;
  /**
   * Use higher values to prefer shorter routes. See [here](#tag/Custom-Model/Customizing-distance_influence) for more details.
   */
  distance_influence?: number;
  /**
   * Areas are given in a GeoJson format. Currently only one format is supported: one object with type Feature, a geometry with type Polygon and optional (but ignored) id and properties fields. See more details and an example [here](#tag/Custom-Model/Define-areas).
   *
   */
  areas?: {
    [key: string]: unknown;
  };
};

export type ProfileRequest = {
  /**
   * The built-in profile your new custom profile shall be based on.
   */
  profile?:
    | "foot"
    | "bike"
    | "ecargobike"
    | "car"
    | "small_truck"
    | "truck"
    | "scooter";
  bounds?: BBox | FeatureCollection;
  custom_model?: CustomModelForProfile;
};

export type ProfileResponse = {
  /**
   * The built-in profile this custom profile is based on.
   */
  profile?: string;
  bounds?: {
    /**
     * A rectangular area given as an array [minLon, minLat, maxLon, maxLat]. The profile will only work in this area.
     */
    bbox?: Array<number>;
  };
  custom_model?: CustomModelForProfile;
  /**
   * The name of the created profile. Use this as the `profile` parameter for the /route API etc. For route optimization requests you need to define a vehicle_type where you can enter the custom profile.
   */
  id?: string;
};

/**
 * The existing custom profiles including their ids
 */
export type ProfileGetResponse = Array<ProfileResponse>;

export type RouteResponse = {
  paths?: Array<RouteResponsePath>;
  info?: ResponseInfo;
};

/**
 * A polyline-encoded list of positions. You'll need to decode this string in client code. We provide open source code in [Java](https://github.com/graphhopper/graphhopper/blob/e649aaed8d3f4378bf2d8889bbbc2318261eabb2/web-api/src/main/java/com/graphhopper/http/WebHelper.java#L54) and [JavaScript](https://github.com/graphhopper/directions-api-js-client/blob/cf43d1a5bc93a3e8007a44fcfc551117e4fa49bc/src/GHUtil.js#L27).
 */
export type EncodedLineString = string;

export type BBox = {
  /**
   * A rectangular area given as an array [minLon, minLat, maxLon, maxLat]. The created profile will only work in this area. The maximum area size is 160 000 square kilometers.
   */
  bbox?: Array<number>;
};

export type FeatureCollection = {
  type?: "FeatureCollection";
  /**
   * A FeatureCollection is an array of type "Feature" from GeoJSON
   */
  features?: Array<{
    type?: "Feature";
    geometry?: Polygon;
  }>;
};

export type RouteResponsePath = {
  /**
   * The total distance, in meters. To get this information for one 'leg' please read [this blog post](https://www.graphhopper.com/blog/2019/11/28/routing-api-using-path-details/).
   *
   */
  distance?: number;
  /**
   * The total travel time, in milliseconds. To get this information for one 'leg' please read [this blog post](https://www.graphhopper.com/blog/2019/11/28/routing-api-using-path-details/).
   *
   */
  time?: number;
  /**
   * The total ascent, in meters.
   *
   */
  ascend?: number;
  /**
   * The total descent, in meters.
   *
   */
  descend?: number;
  points?: unknown & (EncodedLineString | LineString);
  snapped_waypoints?: unknown & (EncodedLineString | LineString);
  /**
   * Whether the `points` and `snapped_waypoints` fields are polyline-encoded strings rather than JSON arrays
   * of coordinates. See the field description for more information on the two formats.
   *
   */
  points_encoded?: boolean;
  /**
   * The bounding box of the route geometry. Format: `[minLon, minLat, maxLon, maxLat]`.
   *
   */
  bbox?: Array<number>;
  /**
   * The instructions for this route. This feature is under active development, and our instructions can sometimes be misleading,
   * so be mindful when using them for navigation.
   *
   */
  instructions?: Array<{
    /**
     * A description what the user has to do in order to follow the route. The language depends on the locale parameter.
     */
    text?: string;
    /**
     * The name of the street to turn onto in order to follow the route.
     */
    street_name?: string;
    /**
     * The optional heading for this roundabout instruction.
     */
    heading?: number;
    /**
     * The optional last heading for this roundabout instruction.
     */
    last_heading?: number;
    /**
     * The distance for this instruction, in meters.
     */
    distance?: number;
    /**
     * The duration for this instruction, in milliseconds.
     */
    time?: number;
    /**
     * Two indices into `points`, referring to the beginning and the end of the segment of the route
     * this instruction refers to.
     *
     */
    interval?: Array<number>;
    /**
     * A number which specifies the sign to show:
     *
     * | sign | description  |
     * |---|---|
     * |-98| an U-turn without the knowledge if it is a right or left U-turn |
     * | -8| a left U-turn |
     * | -7| keep left |
     * | -6| **not yet used**: leave roundabout |
     * | -3| turn sharp left |
     * | -2| turn left |
     * | -1| turn slight left |
     * |  0| continue on street |
     * |  1| turn slight right |
     * |  2| turn right |
     * |  3| turn sharp right |
     * |  4| the finish instruction before the last point |
     * |  5| the instruction before a via point |
     * |  6| the instruction before entering a roundabout |
     * |  7| keep right |
     * |  8| a right U-turn |
     * |  *| **For future compatibility** it is important that all clients are able to handle also unknown instruction sign numbers
     *
     */
    sign?: number;
    /**
     * Only available for roundabout instructions (sign is 6). The count of exits at which the route leaves the roundabout.
     *
     */
    exit_number?: number;
    /**
     * Only available for roundabout instructions (sign is 6). The radian of the route within the roundabout `0 < r < 2*PI` for clockwise and
     * `-2*PI < r < 0` for counterclockwise turns.
     *
     */
    turn_angle?: number;
  }>;
  /**
   * Details, as requested with the `details` parameter. Consider the value `{"street_name": [[0,2,"Frankfurter Straße"],[2,6,"Zollweg"]]}`.
   * In this example, the route uses two streets: The first, Frankfurter Straße, is
   * used between `points[0]` and `points[2]`, and the second, Zollweg, between `points[2]` and `points[6]`.
   * Read more about the usage of path details [here](https://discuss.graphhopper.com/t/2539).
   *
   */
  details?: {
    [key: string]: unknown;
  };
  /**
   * An array of indices (zero-based), specifiying the order in which the input points are visited.
   * Only present if the `optimize` parameter was used.
   *
   */
  points_order?: Array<number>;
};

/**
 * Additional information for your request
 */
export type ResponseInfo = {
  /**
   * Attribution according to our documentation is necessary if no white-label option included.
   */
  copyrights?: Array<string>;
  took?: number;
};

export type IsochroneResponse = {
  /**
   * The list of polygons in GeoJson format. It can be used e.g. in the Leaflet framework:
   *
   * ```
   * L.geoJson(json.polygons).addTo(map)
   * ```
   *
   * The number of polygon is identical to the specified buckets in the query. Every polygon contains the bucket number in the properties section of the GeoJson.
   *
   */
  polygons?: Array<IsochroneResponsePolygon>;
};

/**
 * A found path
 */
export type IsochroneResponsePolygon = {
  properties?: {
    bucket?: number;
  };
  type?: string;
  geometry?: Polygon;
};

export type LineString = {
  type?: string;
  /**
   * A list of coordinate pairs or triples, `[lon,lat]` or `[lon,lat,elevation]`.
   *
   */
  coordinates?: Array<Array<number>>;
};

export type Polygon = {
  type?: "Polygon";
  coordinates?: Array<Array<Array<number>>>;
};

export type GhError = {
  message?: string;
  /**
   * Optional error information.
   */
  hints?: Array<{
    message?: string;
  }>;
};

export type JobId = {
  /**
   * UUID. Unique id for your job/request with which you can fetch your solution
   */
  job_id?: string;
};

export type BadRequest = {
  /**
   * Short error message
   */
  message?: string;
  /**
   * Optional error information.
   */
  hints?: Array<ErrorMessage>;
  /**
   * status
   */
  status?: string;
};

export type ErrorMessage = {
  /**
   * error message
   */
  message?: string;
  /**
   * Details
   */
  details?: string;
};

export type InternalErrorMessage = {
  code?: number;
  /**
   * Details
   */
  message?: string;
};

export type Request = {
  /**
   * Specifies the available vehicles.
   */
  vehicles?: Array<Vehicle>;
  /**
   * Specifies the available vehicle types. These types can be assigned to vehicles.
   */
  vehicle_types?: Array<VehicleType>;
  /**
   * Specifies the orders of the type "service". These are, for example, pickups, deliveries, or other stops to be visited by the specified vehicles. Each of these orders contains only one location.
   */
  services?: Array<Service>;
  /**
   * Specifies the available shipments. Each shipment consists of a pickup and a delivery. For a single shipment, the pickup must always occur before the delivery. However, pickups and deliveries from multiple shipments can be sequenced independently.
   */
  shipments?: Array<Shipment>;
  /**
   * Defines additional relationships between orders.
   */
  relations?: Array<JobRelation | GroupRelation>;
  algorithm?: Algorithm;
  /**
   * Specifies an objective function. The vehicle routing problem is solved in such a way that this objective function is minimized.
   */
  objectives?: Array<Objective>;
  /**
   * Specifies your own transport time and distance matrices.
   */
  cost_matrices?: Array<CostMatrix>;
  /**
   * Specifies general configurations.
   */
  configuration?: Configuration;
};

export type Vehicle = {
  /**
   * Specifies the ID of the vehicle. Ids must be unique, i.e. if there are two vehicles with the same ID, an error is returned.
   */
  vehicle_id: string;
  /**
   * The type ID assigns a vehicle type to this vehicle. You can specify types in the array of vehicle types. If you omit the type ID, the default type is used. The default type is a `car` with a capacity of 0.
   */
  type_id?: string;
  /**
   * An array of shift configurations for this vehicle.
   */
  shifts?: Array<Shift>;
  start_address?: Address;
  /**
   * If this is omitted AND return_to_depot is true then the vehicle needs to return to its start_address.
   */
  end_address?: Address;
  break?: TimeWindowBreak | DriveTimeBreak;
  /**
   * If it is false, the algorithm decides where to end the vehicle route. It ends in one of your customers' locations. The end is chosen such that it contributes to the overall objective function, e.g. min transport_time. If it is true, you can either specify a specific end location (which is then regarded as end depot) or you can leave it and the driver returns to its start location.
   */
  return_to_depot?: boolean;
  /**
   * Earliest start of vehicle in seconds. It is recommended to use the unix timestamp.
   */
  earliest_start?: number;
  /**
   * Latest end of vehicle in seconds, i.e. the time the vehicle needs to be at its end location at latest. Omit this field to indicate no limit.
   */
  latest_end?: number;
  /**
   * Latest start of vehicle in seconds. If specified and greater than earliest_start, the optimizer determines the best departure time within the [earliest_start, latest_start] window to minimize waiting time. (beta)
   */
  latest_start?: number;
  /**
   * Controls departure preference when latest_start is set. "late" (default) departs as late as possible while meeting all time windows. "early" departs as early as possible, eliminating waiting time along the route. Ignored without latest_start. (beta)
   */
  departure_preference?: "early" | "late";
  /**
   * An array of skills, i.e. an array of strings (not case sensitive).
   */
  skills?: Array<string>;
  /**
   * Specifies the maximum distance (in meters) a vehicle can go.
   */
  max_distance?: number;
  /**
   * Specifies the maximum driving time (in seconds) a vehicle/driver is allowed, i.e. the maximum time spent on the road (service and waiting times are not included).
   */
  max_driving_time?: number;
  /**
   * Specifies the maximum number of jobs a vehicle can load.
   */
  max_jobs?: number;
  /**
   * Specifies the minimum number of jobs a vehicle should load. This is a soft constraint, i.e. if it is not possible to fulfill “min_jobs”, we will still try to get as close as possible to this constraint.
   */
  min_jobs?: number;
  /**
   * Specifies the maximum number of activities a vehicle can conduct.
   */
  max_activities?: number;
  /**
   * Indicates whether a vehicle should be moved even though it has not been assigned any jobs.
   */
  move_to_end_address?: boolean;
};

/**
 * The routing profile. It determines the network, speed and other physical attributes used when computing the route. See the section about [routing profiles](#tag/Map-Data-and-Routing-Profiles) for more details and valid profile values.
 */
export type VehicleProfileId = string;

/**
 * Shift of vehicle.
 */
export type Shift = {
  /**
   * A unique identifier for this vehicle's shift.
   */
  shift_id?: string;
  start_address?: Address;
  /**
   * If this is omitted AND return_to_depot is true then the vehicle needs to return to its start_address.
   */
  end_address?: Address;
  /**
   * Earliest start of vehicle in seconds. It is recommended to use the unix timestamp.
   */
  earliest_start?: number;
  /**
   * Latest end of vehicle in seconds, i.e. the time the vehicle needs to be at its end location at latest. Omit this field to indicate no limit.
   */
  latest_end?: number;
  /**
   * Latest start of vehicle in seconds. If specified and greater than earliest_start, the optimizer determines the best departure time within the [earliest_start, latest_start] window to minimize waiting time. (beta)
   */
  latest_start?: number;
  /**
   * Controls departure preference when latest_start is set. "late" (default) departs as late as possible while meeting all time windows. "early" departs as early as possible, eliminating waiting time along the route. Ignored without latest_start. (beta)
   */
  departure_preference?: "early" | "late";
  /**
   * If true, vehicle returns to its start location (or specified end location). If false, vehicle can end at any customer location that optimizes the objective function.
   */
  return_to_depot?: boolean;
  break?: TimeWindowBreak | DriveTimeBreak;
};

/**
 * Use `objectives` instead.
 *
 * @deprecated
 */
export type Algorithm = {
  problem_type?: "min" | "min-max";
  objective?: "transport_time" | "completion_time";
};

export type Address = {
  /**
   * Specifies the id of the location.
   */
  location_id: string;
  /**
   * Name of location.
   */
  name?: string;
  /**
   * Longitude of location.
   */
  lon: number;
  /**
   * Latitude of location.
   */
  lat: number;
  /**
   * Optional parameter. Specifies a hint containing the street name to better snap the coordinates (lon,lat) to the road network. For example, if an address or building has two or more neighboring streets, you can control which street is used to find the closest location.
   */
  street_hint?: string;
  /**
   * Optional parameter. Specifies on which side a point should be relative to the driver when they leave/arrive at a start/target/via point. Only supported for motor vehicles and OpenStreetMap.
   * If you would like to arrive at this address without having to cross the street, use `curbside=right/left` for countries with right/left-hand driving. Using `curbside=any` is the same as not specifying this parameter at all.
   *
   */
  curbside?: "right" | "left" | "any";
};

/**
 * Address of activity
 */
export type ResponseAddress = {
  /**
   * Specifies the id of the location.
   */
  location_id?: string;
  /**
   * Name of location.
   */
  name?: string;
  /**
   * Longitude of location.
   */
  lon?: number;
  /**
   * Latitude of location.
   */
  lat?: number;
  /**
   * Optional parameter. Specifies a hint for each address to better snap the coordinates (lon,lat) to road network. E.g. if there is an address or house with two or more neighboring streets you can control for which street the closest location is looked up.
   */
  street_hint?: string;
  snapped_waypoint?: SnappedWaypoint;
};

/**
 * Access point to the road network. It is only available if `return_snapped_waypoints` is true (by default it is false).
 */
export type SnappedWaypoint = {
  /**
   * Longitude of location.
   */
  lon?: number;
  /**
   * Latitude of location.
   */
  lat?: number;
};

export type DriveTimeBreak = {
  /**
   * Specifies the duration of the break in seconds.
   */
  duration: number;
  /**
   * Specifies the max driving time (in a row) without break in seconds.
   */
  max_driving_time: number;
  /**
   * Specifies the initial (current) driving time of a driver to allow dynamic adaptations in seconds.
   */
  initial_driving_time?: number;
  /**
   * Array specifying how a break duration (in seconds) can be split into several smaller breaks
   */
  possible_split?: Array<number>;
};

export type TimeWindowBreak = {
  /**
   * Specifies the earliest start time of the break in seconds.
   */
  earliest: number;
  /**
   * Specifies the latest start time of break in seconds.
   */
  latest: number;
  /**
   * Specifies the duration of the break in seconds.
   */
  duration: number;
};

export type VehicleType = {
  /**
   * Specifies the id of the vehicle type. If a vehicle needs to be of this type, it should refer to this with its type_id attribute.
   */
  type_id: string;
  profile?: VehicleProfileId;
  /**
   * Specifies an array of capacity dimension values which need to be int values. For example, if there are two dimensions such as volume and weight then it needs to be defined as [ 1000, 300 ] assuming a maximum volume of 1000 and a maximum weight of 300.
   */
  capacity?: Array<number>;
  /**
   * Specifies a speed factor for this vehicle type. For example, if a vehicle using this type should travel at half the speed calculated by our routing engine, set the speed factor to 0.5.
   */
  speed_factor?: number;
  /**
   * Specifies a service time factor for this vehicle type. For example, if a vehicle/driver using this type can complete services twice as fast as specified in the service or shipment, set this to 0.5.
   */
  service_time_factor?: number;
  /**
   * **_BETA feature_**! Cost parameter per distance unit, here meter is used
   */
  cost_per_meter?: number;
  /**
   * **_BETA feature_**! Cost parameter per time unit, here second is used
   */
  cost_per_second?: number;
  /**
   * **_BETA feature_**! Cost parameter vehicle activation, i.e. fixed costs per vehicle
   */
  cost_per_activation?: number;
  /**
   * Specifies whether traffic should be considered. If "tomtom" is used and this is false, free-flow travel times from TomTom are calculated. If this is true, historical traffic information is used. Traffic data is not yet available for "openstreetmap", so setting this to true has no effect when using OpenStreetMap.
   */
  consider_traffic?: boolean;
  /**
   * Specifies the network data provider. Either use [`openstreetmap`](#tag/Map-Data-and-Routing-Profiles/OpenStreetMap) (default) or [`tomtom`](#tag/Map-Data-and-Routing-Profiles/TomTom) (add-on required).
   */
  network_data_provider?: "openstreetmap" | "tomtom";
};

export type Service = {
  /**
   * Specifies the id of the service. Ids need to be unique so there must not be two services/shipments with the same id.
   */
  id: string;
  /**
   * Specifies type of service. This makes a difference if items are loaded or unloaded, i.e. if one of the size dimensions > 0. If it is specified as `service` or `pickup`, items are loaded and will stay in the vehicle for the rest of the route (and thus consumes capacity for the rest of the route). If it is a `delivery`, items are implicitly loaded at the beginning of the route and will stay in the route until delivery (and thus releases capacity for the rest of the route).
   */
  type?: "service" | "pickup" | "delivery";
  /**
   * Specifies the priority. Can be 1 = high priority to 10 = low priority. Priority only affects the solution when not all services can be assigned  (e.g., due to insufficient vehicle capacity, conflicting time windows,  or vehicle constraints). In these cases, the optimizer preferentially  assigns high-priority services and leaves low-priority services unassigned.  The lower the priority, the earlier these services are omitted from  the solution. When all services can be served within the given constraints,  priority has no effect - all services are assigned regardless of their  priority values, and the optimizer focuses solely on the specified objective  (e.g., minimizing distance or number of vehicles).
   *
   */
  priority?: number;
  /**
   * A meaningful name for the service, e.g. `"deliver pizza"`.
   */
  name?: string;
  address?: Address;
  /**
   * Specifies the duration of the service in seconds, i.e. how long it takes to perform the actual service. This time is charged for each activity—if multiple services occur at the same location, their durations are summed.
   */
  duration?: number;
  /**
   * Specifies the preparation time in seconds. This time is added after arrival and before the service can begin, modeling tasks like finding a parking spot. Preparation time is only charged once when the location differs from the previous stop—if multiple consecutive activities share the same location, it is applied only to the first.
   */
  preparation_time?: number;
  /**
   * Specifies the setup time in seconds. This is a fixed overhead charged when arriving at a new location (e.g., dock check-in, security clearance, equipment preparation). Unlike `duration`, setup time is only charged once per location—if multiple consecutive activities share the same location, it is applied only to the first. The activity timeline is: arrival → preparation_time → waiting (if early) → setup_time → duration.
   */
  setup_time?: number;
  /**
   * Specifies an array of time window objects (see TimeWindow object below). Specify the time either with the recommended Unix timestamp (the number of seconds since 1970-01-01) or you can count the seconds relative to Monday morning 00:00 and define the whole week in seconds. For example, Monday 9am is represented by 9 hours * 3600 sec/hour = 32400. Wednesday 1pm corresponds to 2 days * 24 hours/day * 3600 sec/hour + 13 hours * 3600 sec/hour = 219600. See our tutorial for more information.
   */
  time_windows?: Array<TimeWindow>;
  /**
   * Size can have multiple dimensions and should correspond to the capacity dimension array of the vehicle type. For example, if the item that needs to be delivered has two size dimensions, volume and weight, specify it as follows: [ 20, 5 ], assuming a volume of 20 and a weight of 5.
   */
  size?: Array<number>;
  /**
   * Specifies an array of required skills, i.e. an array of strings (not case sensitive). For example, if this service needs to be performed by a technician with a `drilling_machine` and a `screw_driver`, specify the array as follows: `["drilling_machine","screw_driver"]`. This means that the service can only be done by a vehicle (technician) that has both `drilling_machine` AND `screw_driver` in its skill array. Otherwise, it remains unassigned.
   */
  required_skills?: Array<string>;
  /**
   * Specifies an array of allowed vehicles, i.e. an array of vehicle IDs. For example, if this service can only be performed by EITHER `technician_peter` OR `technician_stefan`, specify this as follows: `["technician_peter","technician_stefan"]`.
   */
  allowed_vehicles?: Array<string>;
  /**
   * Specifies an array of disallowed vehicles, i.e. an array of vehicle IDs.
   */
  disallowed_vehicles?: Array<string>;
  /**
   * Specifies an array of preferred vehicles.
   */
  preferred_vehicles?: Array<PreferredVehicle>;
  /**
   * Specifies the maximum time in seconds a delivery can stay in the vehicle. Currently, it only works with services of "type":"delivery". Omit this field to indicate no limit.
   */
  max_time_in_vehicle?: number;
  /**
   * Group this service belongs to. Orders with the same group label are treated as a contiguous cluster — they stay together as a block, but the optimizer picks the internal sequence. Groups can also be referenced in group-level relations to enforce constraints like `in_sequence` across entire clusters.
   */
  group?: string;
};

export type Shipment = {
  /**
   * Specifies the ID of the shipment. IDs must be unique, so there must not be two services/shipments with the same ID.
   */
  id: string;
  /**
   * A meaningful name for the shipment, e.g. "pick up and deliver pizza to Peter".
   */
  name?: string;
  /**
   * Specifies the priority. Can be 1 = high priority to 10 = low priority. Priority only affects the solution when not all shipments can be assigned  (e.g., due to insufficient vehicle capacity, conflicting time windows,  or vehicle constraints). In these cases, the optimizer preferentially  assigns high-priority shipments and leaves low-priority shipments unassigned.  The lower the priority, the earlier these shipments are omitted from  the solution. When all shipments can be served within the given constraints,  priority has no effect - all shipments are assigned regardless of their  priority values, and the optimizer focuses solely on the specified objective  (e.g., minimizing distance or number of vehicles).
   *
   */
  priority?: number;
  pickup: Stop;
  delivery: Stop;
  /**
   * Size can have multiple dimensions and should correspond to the capacity dimension array of the vehicle type. For example, if the item that needs to be delivered has two size dimensions, volume and weight, specify it as follows: [ 20, 5 ], assuming a volume of 20 and a weight of 5.
   */
  size?: Array<number>;
  /**
   * Specifies an array of required skills, i.e. an array of strings (not case sensitive). For example, if this shipment needs to be handled by a technician with a `drilling_machine` and a `screw_driver`, specify the array as follows: `["drilling_machine","screw_driver"]`. This means that the shipment can only be done by a vehicle (technician) that has both `drilling_machine` AND `screw_driver` in its skill array. Otherwise, it remains unassigned.
   */
  required_skills?: Array<string>;
  /**
   * Specifies an array of allowed vehicles, i.e. an array of vehicle IDs. For example, if this shipment can only be handled by EITHER "technician_peter" OR "technician_stefan", specify this as follows: ["technician_peter","technician_stefan"].
   */
  allowed_vehicles?: Array<string>;
  /**
   * Specifies an array of disallowed vehicles, i.e. an array of vehicle IDs.
   */
  disallowed_vehicles?: Array<string>;
  /**
   * Specifies an array of preferred vehicles.
   */
  preferred_vehicles?: Array<PreferredVehicle>;
  /**
   * Specifies the maximum time in seconds a shipment can stay in the vehicle. Omit this field to indicate no limit.
   */
  max_time_in_vehicle?: number;
};

export type Stop = {
  /**
   * Specifies pickup or delivery address.
   */
  address?: Address;
  /**
   * Specifies the duration of the pickup or delivery in seconds, e.g. how long it takes to load or unload items. This time is charged for each stop—if multiple pickups or deliveries occur at the same location, their durations are summed.
   */
  duration?: number;
  /**
   * Specifies the preparation time in seconds. This time is added after arrival and before the pickup or delivery can begin, modeling tasks like finding a parking spot. Preparation time is only charged once when the location differs from the previous stop—if multiple consecutive activities share the same location, it is applied only to the first.
   */
  preparation_time?: number;
  /**
   * Specifies the setup time in seconds. This is a fixed overhead charged when arriving at a new location (e.g., dock check-in, security clearance, equipment preparation). Unlike `duration`, setup time is only charged once per location—if multiple consecutive activities share the same location, it is applied only to the first. The activity timeline is: arrival → preparation_time → waiting (if early) → setup_time → duration.
   */
  setup_time?: number;
  /**
   * Specifies an array of time window objects (see time window object below). For example, if an item needs to be delivered between 7am and 10am then specify the array as follows: [ { "earliest": 25200, "latest" : 32400 } ] (starting the day from 0 in seconds).
   */
  time_windows?: Array<TimeWindow>;
  /**
   * Group this stop belongs to. Orders with the same group label are treated as a contiguous cluster — they stay together as a block, but the optimizer picks the internal sequence. Groups can also be referenced in group-level relations to enforce constraints like `in_sequence` across entire clusters.
   */
  group?: string;
};

export type TimeWindow = {
  /**
   * Specifies the opening time of the time window in seconds, i.e. the earliest time the service can start.
   */
  earliest?: number;
  /**
   * Specifies the closing time of the time window in seconds, i.e. the latest time the service can start. Omit this field to indicate no limit.
   */
  latest?: number;
};

export type PreferredVehicle = {
  /**
   * Id of the preferred vehicle.
   */
  vehicle_id?: string;
  /**
   * Number between 1 and 10 which indicates the priority of the preferred vehicle. 1 indicates the highest priority, 10 the lowest.
   */
  priority?: number;
};

/**
 * Specifies general configurations that are taken into account when solving the vehicle routing problem.
 */
export type Configuration = {
  routing?: Routing;
  optimization?: Optimization;
};

/**
 * This contains all optimization specific configurations.
 */
export type Optimization = {
  /**
   * If you use groups, you sometimes want to place orders without group assignment in the best position, i.e. sometimes in the middle of a group and not before or after the group. This is not allowed by default. However, if this field here is "true", these orders (without a group assignment) can be inserted freely.
   */
  free_insertion?: boolean;
  /**
   * When `free_insertion` is enabled, ungrouped orders can be inserted anywhere, including inside groups. Use this property to specify groups where ungrouped orders should NOT be inserted. This is useful when you have groups like "asap" (must be served first) or "last" (must be served last) that should stay compact. For example, with `free_insertion_exempt_groups: ["asap", "last"]`, ungrouped orders can be inserted freely between other groups, but not inside the "asap" or "last" groups, and not between start and "asap" or between "last" and end when using `in_direct_sequence` relations with these groups.
   */
  free_insertion_exempt_groups?: Array<string>;
  /**
   * Radius in meters. When set to a positive value, ungrouped jobs are automatically assigned to the nearest existing group based on road distance. This is useful when you use groups and group-level `in_sequence` to define an area visit order, and want new jobs to automatically cluster into the right area. Jobs outside the radius of all groups remain ungrouped. Requires `free_insertion: true` for ungrouped jobs to be placed freely.
   */
  auto_assign_radius?: number;
};

/**
 * This contains all routing specific configurations.
 */
export type Routing = {
  /**
   * It lets you specify whether the API should provide you with route geometries for vehicle routes or not. Thus, you do not need to do extra routing to get the polyline for each route.
   */
  calc_points?: boolean;
  /**
   * Indicates whether historical traffic information should be considered.
   */
  consider_traffic?: boolean;
  /**
   * Specifies the data provider. Read more about it [here](#tag/Map-Data-and-Routing-Profiles).
   */
  network_data_provider?: "openstreetmap" | "tomtom";
  /**
   * In some cases curbside constraints cannot be fulfilled. For example in one-way streets you cannot arrive at a building that is on the left side of the street such that the building is to the right of you (unless you drove the one-way street the wrong/illegal way). You can set the `curbside_strictness` to `soft` to ignore the curbside constraint in such cases or set it to `strict` to get an error response instead. You can also set it to `ignore` to ignore all curbside constraints (this is useful to compare the results with and without constraints without modifying every single address).
   */
  curbside_strictness?: "ignore" | "soft" | "strict";
  /**
   * Indicates whether matrix calculation should fail immediately when points cannot be connected.
   */
  fail_fast?: boolean;
  /**
   * Indicates whether the solution should include snapped waypoints. Unlike the address coordinates, a snapped waypoint is the access point to the road network.
   */
  return_snapped_waypoints?: boolean;
  /**
   * Optional parameter. 'Snapping' is the process of finding the closest road location for GPS coordinates provided in the `points` array. The `snap_preventions` array allows you to prevent snapping to specific types of roads. For example, if the array includes `bridge`, then the routing engine will avoid snapping to a bridge, even if it is the closest road for the given point. Note that once snapped the routing algorithm can still route over bridges (or the other values). To avoid this you need to use the `custom_model`.
   */
  snap_preventions?: Array<
    "motorway" | "trunk" | "bridge" | "ford" | "tunnel" | "ferry"
  >;
};

export type Objective = {
  /**
   * Type of objective function, i.e. `min`, `min-max`, or `balance`.
   *
   * * `min`: Minimizes the objective value.
   * * `min-max`: Minimizes the maximum objective value.
   * * `balance`: **(Beta)** Optimizes for balanced workloads across drivers. Use with `value: completion_time` to balance working time, or `value: activities` to balance the number of stops per driver.
   *
   * For instance, `min` -> `vehicles` minimizes the number of employed vehicles.
   * `min` -> `completion_time` minimizes the sum of your vehicle routes' completion time.
   *
   * If you use, for example, `min-max` -> `completion_time`, it minimizes the maximum of your vehicle routes' completion time, i.e. it minimizes the overall makespan.
   * This only makes sense if you have more than one vehicle. In case of one vehicle, switching from `min` to `min-max` should not have any impact.
   * If you have more than one vehicle, then the algorithm tries to constantly move stops from one vehicle to another such that
   * the completion time of longest vehicle route can be further reduced. For example, if you have one vehicle that takes 8 hours
   * to serve all customers, adding another vehicle (and using `min-max`) might halve the time to serve all customers to 4 hours. However,
   * this usually comes with higher transport costs.
   *
   * If you want to minimize `vehicles` first and, second, `completion_time`, you can also combine different objectives like this:
   *
   * ```json
   * "objectives" : [
   * {
   * "type": "min",
   * "value": "vehicles"
   * },
   * {
   * "type": "min",
   * "value": "completion_time"
   * }
   * ]
   * ```
   *
   * If you want to balance activities or the number of stops among all employed drivers, you need to specify it as follows:
   *
   * ```json
   * "objectives" : [
   * {
   * "type": "min-max",
   * "value": "completion_time"
   * },
   * {
   * "type": "min-max",
   * "value": "activities"
   * }
   * ]
   * ```
   *
   * #### Balance Objective (Beta)
   *
   * The `balance` objective type provides a more sophisticated approach to workload balancing compared to `min-max`.
   * While `min-max` only minimizes the maximum route value, `balance` optimizes for equal distribution of work across all drivers
   * by minimizing the coefficient of variation (CV) of workloads.
   *
   * Use `balance` with:
   * * `value: completion_time` - Balance working time (service time + travel time + waiting time) across drivers
   * * `value: activities` - Balance the number of stops per driver
   *
   * The optional `level` parameter (1-3) controls how strongly to prioritize balance:
   * * `level: 1` - Mild balance preference, allows some variation for better travel efficiency
   * * `level: 2` - Moderate balance (default), good trade-off between balance and efficiency
   * * `level: 3` - Strict balance, prioritizes equal workloads even at the cost of longer travel
   *
   * Example:
   * ```json
   * "objectives" : [
   * {
   * "type": "balance",
   * "value": "completion_time",
   * "level": 2
   * }
   * ]
   * ```
   *
   * **When to use `balance` vs `min-max`:**
   * * Use `balance` when you want all drivers to have similar workloads (fairness)
   * * Use `min-max` when you only care about minimizing the longest route (makespan)
   *
   * **Note:** The `balance` objective currently only supports services. Requests that combine `balance` objectives with shipments will return a `400` error.
   *
   */
  type: "min" | "min-max" | "balance";
  /**
   * The value of the objective function.
   * The objective value `transport_time` solely considers the time
   * your drivers spend on the road, i.e. transport time. In contrary to `transport_time`, `completion_time` also takes waiting times at customer sites into account.
   * The `completion_time` of a route is defined as the time from starting to ending the route,
   * i.e. the route's transport time, the sum of waiting times plus the sum of activity durations.
   * The `completion_time_last_stop`, on the other hand, refers to the completion time of the very last order in a tour or, in other words,
   * the completion time without the last section from the last stop to the end of the tour.
   * This is typically used if the orders are to be processed as quickly as possible.
   * If you solely want to minimize route duration, you should use simply use `route_duration`.
   * The objective value `vehicles` can only be used along with `min` and minimizes vehicles.
   *
   * For `balance` objective type, only `completion_time` and `activities` are supported.
   *
   */
  value:
    | "completion_time"
    | "completion_time_last_stop"
    | "route_duration"
    | "transport_time"
    | "vehicles"
    | "activities";
  /**
   * **(Beta)** Level for `balance` objectives (1-3). Controls how strongly to prioritize balanced workloads.
   *
   * * `1` - Accept some workload variation for better travel efficiency
   * * `2` - Moderate balance (default)
   * * `3` - Strictly equal workloads, even at cost of longer travel
   *
   * Only valid when `type` is `balance`. Ignored for other objective types.
   *
   */
  level?: number;
};

export type CostMatrix = {
  /**
   * Type of cost matrix. Currently "default" and "google" are supported.
   */
  type?: "default" | "google";
  location_ids?: Array<string>;
  /**
   * JSON data of matrix response
   */
  data?: {
    times?: Array<Array<number>>;
    distances?: Array<Array<number>>;
    /**
     * Additional information for your request
     */
    info?: {
      copyrights?: Array<string>;
      took?: number;
    };
  };
  /**
   * Vehicle profile, or empty for a catch-all fallback.
   */
  profile?: string;
};

export type GroupRelation = {
  /**
   * Specifies the type of relation. It must be either of type `in_sequence`, `in_direct_sequence` or `neighbor`.
   *
   */
  type: string;
  /**
   * An array of groups that should be related
   */
  groups: Array<string>;
};

export type JobRelation = {
  /**
   * Specifies the type of relation. It must be either of type `in_same_route`, `not_in_same_route`, `in_sequence`, `in_direct_sequence`, `neighbor` or `in_area_sequence`.
   *
   * `in_same_route`: As the name suggests, it enforces the specified services or shipments to be in the same route. It can be specified as follows:
   *
   * ```json
   * {
   * "type": "in_same_route",
   * "ids": ["serv_i_id","serv_j_id"]
   * }
   * ```
   *
   * This enforces service i to be in the same route as service j no matter which vehicle will be employed. If a specific vehicle (driver) is required to conduct this, just add a `vehicle_id` like this:
   *
   * ```
   * {
   * "type": "in_same_route",
   * "ids": ["serv_i_id","serv_j_id"],
   * "vehicle_id": "vehicle1"
   * }
   * ```
   *
   * This not only enforces service i and j to be in the same route, but also ensures that both services are in the route of `vehicle1`.
   *
   * *Tip*: This way initial loads and vehicle routes can be modelled. For example, if your vehicles are already on the road and new orders come in, then vehicles can still be rescheduled subject to the orders that have already been assigned to these vehicles.
   *
   * `not_in_same_route`: It ensures that 2 or more orders are not transported by the same vehicle. It can be specified as follows:
   *
   * ```json
   * {
   * "type": "not_in_same_route",
   * "ids": ["serv_i_id","serv_j_id"]
   * }
   * ```
   *
   *
   * `in_sequence`: This relation type enforces n jobs to be in sequence. It can be specified as
   *
   * ```json
   * {
   * "type": "in_sequence",
   * "ids": ["serv_i_id","serv_j_id"]
   * }
   * ```
   *
   * which means that service j needs to be in the same route as service i AND it needs to occur somewhere after service i. As described above, if a specific vehicle needs to perform these services, just add `vehicle_id`.
   *
   *
   * `in_direct_sequence`: This enforces n services or shipments to be in direct sequence. It can be specified as
   *
   * ```json
   * {
   * "type": "in_direct_sequence",
   * "ids": ["serv_i_id","serv_j_id","serv_k_id"]
   * }
   * ```
   *
   * This causes service j to occur directly after service i, and service k to occur directly after service j, i.e. in strict sequence. Again, a specific vehicle can be assigned by adding a `vehicle_id` to the relation.
   *
   * `neighbor`: This specifies a neighbor relationship, i.e., if services i and j are to be neighbors, i must be either immediately before or after j. It can be specified as follows:
   *
   * ```json
   * {
   * "type": "neighbor",
   * "ids": ["serv_i_id","serv_j_id"]
   * }
   * ```
   *
   * `in_area_sequence`: This defines an area-level visit order. You pick one job per area as an anchor and set a radius. All other jobs within the radius of an anchor are automatically clustered into that area. Areas are visited in the specified order, and within each area the optimizer handles the details. It can be specified as follows:
   *
   * ```json
   * {
   * "type": "in_area_sequence",
   * "ids": ["job_area_a","job_area_b","job_area_c"],
   * "area_radius": 5000
   * }
   * ```
   *
   * This means: visit the area around `job_area_a` first, then the area around `job_area_b`, then `job_area_c`. Any ungrouped job within 5000 meters (road distance) of an anchor is automatically assigned to that area. Jobs outside all radii are placed wherever fits best.
   *
   * You can assign the area sequence to a specific vehicle:
   *
   * ```json
   * {
   * "type": "in_area_sequence",
   * "ids": ["job_area_a","job_area_b"],
   * "area_radius": 5000,
   * "vehicle_id": "vehicle1"
   * }
   * ```
   *
   * Multiple `in_area_sequence` relations can be used to assign different area sequences to different vehicles. If an anchor job already has a `group`, that group name is used instead of an auto-generated one, and nearby jobs are assigned to it.
   *
   * *Special IDs*:
   * If you look at the previous example and you want service i to be the first in the route, use the special ID `start` as follows:
   *
   * ```json
   * {
   * "type": "in_direct_sequence",
   * "ids": ["start","serv_i_id","serv_j_id","serv_k_id"]
   * }
   * ```
   *
   * This enforces the direct sequence of i, j, and k at the beginning of the route. If this sequence should be at the end of the route, use the special ID `end` like this:
   *
   * ```json
   * {
   * "type": "in_direct_sequence",
   * "ids": ["serv_i_id","service_j_id","serv_k_id","end"]
   * }
   * ```
   *
   * If you deal with services then you need to use the 'id' of your services in the field 'ids'. To also consider sequences of the pickups and deliveries of your shipments, you need to use a special ID, i.e. use the shipment id plus the keyword `_pickup` or `_delivery`. For example, to ensure that the pickup and delivery of the shipment with the id 'my_shipment' are direct neighbors, you need the following specification:
   *
   * ```
   * {
   * "type": "in_direct_sequence",
   * "ids": ["my_ship_pickup","my_ship_delivery"]
   * }
   * ```
   *
   *
   */
  type: string;
  /**
   * Specifies an array of shipment and/or service ids that are in relation. If you deal with services then you need to use the id of your services in ids. To also consider sequences of the pickups and deliveries of your shipments, you need to use a special ID, i.e. use your shipment id plus the keyword `_pickup` or `_delivery`. If you want to place a service or shipment activity at the beginning of your route, use the special ID `start`. In turn, use `end` to place it at the end of the route.
   */
  ids: Array<string>;
  /**
   * Id of pre-assigned vehicle, i.e. the vehicle id that is determined to conduct the services and shipments in this relation.
   */
  vehicle_id?: string;
  /**
   * Radius in meters. Required for `in_area_sequence` relations. Jobs within this road distance of an anchor job are automatically assigned to that area.
   */
  area_radius?: number;
};

export type Response = {
  copyrights?: Array<string>;
  /**
   * Indicates the current status of the job
   */
  status?: "waiting_in_queue" | "processing" | "finished";
  /**
   * Waiting time in milliseconds.
   */
  waiting_time_in_queue?: number;
  /**
   * Processing time in ms. If job is still waiting in queue, processing_time is 0
   */
  processing_time?: number;
  solution?: Solution;
};

/**
 * Only available if status field indicates `finished`.
 */
export type Solution = {
  /**
   * @deprecated
   */
  costs?: number;
  /**
   * Overall distance traveled in meters, i.e. the sum of each route's transport distance.
   */
  distance?: number;
  /**
   * Use `transport_time` instead.
   *
   * @deprecated
   */
  time?: number;
  /**
   * Overall time travelled in seconds, i.e. the sum of each route's transport time.
   */
  transport_time?: number;
  /**
   * Operation time of longest route in seconds.
   */
  max_operation_time?: number;
  /**
   * Overall waiting time in seconds.
   */
  waiting_time?: number;
  /**
   * Overall service time in seconds.
   */
  service_duration?: number;
  /**
   * Overall preparation time in seconds.
   */
  preparation_time?: number;
  /**
   * Overall setup time in seconds.
   */
  setup_time?: number;
  /**
   * Overall completion time in seconds, i.e. the sum of each route's operation time.
   */
  completion_time?: number;
  /**
   * Number of employed vehicles.
   */
  no_vehicles?: number;
  /**
   * Number of jobs that could not be assigned to final solution.
   */
  no_unassigned?: number;
  /**
   * An array of routes
   */
  routes?: Array<Route>;
  unassigned?: {
    /**
     * An array of ids of unassigned services
     */
    services?: Array<string>;
    /**
     * An array of ids of unassigned shipments
     */
    shipments?: Array<string>;
    /**
     * An array of ids of unassigned breaks
     */
    breaks?: Array<string>;
    /**
     * An array of details, i.e. reason for unassigned services or shipments
     */
    details?: Array<Detail>;
    /**
     * Per-activity breakdown of every unassigned job. For each unassigned **service** there is exactly one entry; for each unassigned **shipment** there are two entries (one with `type: "pickupShipment"` and one with `type: "deliverShipment"`), both sharing the shipment id.
     *
     * Schedule-derived fields (`arr_time`, `end_time`, `waiting_time`, `distance`, `driving_time`, `preparation_time`, `setup_time`, `load_before`, `load_after`) are zero or omitted because the job was never placed on a route. The descriptive fields `name`, `priority`, and `group` are echoed from the original request when present, so consumers can render or audit unassigned stops without joining back to the request. `time_window` is omitted because the solver never picked one.
     *
     */
    activities?: Array<Activity>;
  };
};

export type Detail = {
  /**
   * Id of unassigned service/shipment
   */
  id?: string;
  /**
   * Reason code (most likely reason). See `reasons` array for additional probable reasons.
   *
   * Code   |  Reason
   * :------|:---------
   * 1 | cannot serve required skill
   * 2 | cannot be visited within time window
   * 3 | does not fit into any vehicle due to capacity
   * 4 | cannot be assigned due to max distance constraint of vehicles
   * 21 | could not be assigned due to relation constraint
   * 22 | could not be assigned due to allowed vehicle constraint
   * 23 | could not be assigned due to max-time-in-vehicle constraint
   * 24 | driver does not need a break
   * 25 | could not be assigned due to disallowed vehicle constraint
   * 26 | could not be assigned due to max drive time constraint
   * 27 | could not be assigned due to max job constraint
   * 28 | could not be assigned due to max activity constraint
   * 29 | could not be assigned due to group relation constraint
   * 30 | could not be assigned due to driving time break
   * 50 | underlying location cannot be accessed over road network by at least one vehicle
   * 51 | location is isolated and cannot reach any other location over road network
   *
   */
  code?: number;
  /**
   * Human readable reason as listed above (most likely reason)
   */
  reason?: string;
  /**
   * Array of probable reasons why this job could not be assigned, sorted by likelihood (most likely first).
   * This provides more context than the single `code`/`reason` fields, which only show the most likely reason.
   * For example, a job might fail due to a relation constraint (code 21), but the underlying cause
   * could be a time window violation (code 2). In this case, both reasons would appear in this array.
   *
   */
  reasons?: Array<ReasonEntry>;
};

/**
 * A single reason entry with code and human-readable description
 */
export type ReasonEntry = {
  /**
   * Reason code (see Detail.code for the full list of codes)
   */
  code?: number;
  /**
   * Human readable reason
   */
  reason?: string;
};

export type Route = {
  /**
   * Id of vehicle that operates route
   */
  vehicle_id?: string;
  /**
   * Distance of route in meters.
   */
  distance?: number;
  /**
   * Transport time of route in seconds
   */
  transport_time?: number;
  /**
   * Completion time of route in seconds
   */
  completion_time?: number;
  /**
   * Waiting time of route in seconds
   */
  waiting_time?: number;
  /**
   * Service duration of route in seconds
   */
  service_duration?: number;
  /**
   * Preparation time of route in seconds
   */
  preparation_time?: number;
  /**
   * Setup time of route in seconds
   */
  setup_time?: number;
  /**
   * Array of activities
   */
  activities?: Array<Activity>;
  /**
   * One entry for every vehicle
   */
  points?: Array<unknown & (EncodedLineString | LineString)>;
};

export type Activity = {
  /**
   * type of activity
   */
  type?:
    | "start"
    | "end"
    | "service"
    | "pickupShipment"
    | "deliverShipment"
    | "pickup"
    | "delivery"
    | "break";
  /**
   * Id referring to the underlying service or shipment, i.e. the shipment or service this activity belongs to
   */
  id?: string;
  /**
   * Human-readable name copied from the underlying service or shipment, if one was set on the request. Omitted for `start`/`end` activities and when the request did not specify a name.
   */
  name?: string;
  /**
   * Id that refers to address
   */
  location_id?: string;
  address?: ResponseAddress;
  /**
   * Arrival time at this activity in seconds. If type is `start`, this is not available (since it makes no sense to have `arr_time` at start). However, `end_time` is available and actually means \"departure time\" at start location. It is important to note that `arr_time` does not necessarily mean \"start of underlying activity\", it solely means arrival time at activity location. If this activity has no time windows and if there are no further preparation times, `arr_time` is equal to activity start time.
   */
  arr_time?: number;
  /**
   * End time of and thus departure time at this activity. If type is `end`, this is not available (since it makes no sense to have an `end_time` at end) `end_time` at each activity is equal to the departure time at the activity location.
   */
  end_time?: number;
  /**
   * Translation of unix timestamp in `end_time` to end date time string with zone offset. It will be in the following ISO-8601 format: 2007-12-03T10:15:30+01:00.
   */
  end_date_time?: Date;
  /**
   * Translation of unix timestamp in `arr_time` to arrival date time string with zone offset. It will be in the following ISO-8601 format: 2007-12-03T10:15:30+01:00.
   */
  arr_date_time?: Date;
  /**
   * Waiting time at this activity in seconds. A waiting time can occur if the activity has at least one time window. If `arr_time` < `time_window.earliest` a waiting time of `time_window_earliest` - `arr_time` occurs.
   */
  waiting_time?: number;
  /**
   * preparation time at this activity in seconds
   */
  preparation_time?: number;
  /**
   * Setup time at this activity in seconds. Setup time is charged when the location differs from the previous activity, representing location overhead like dock check-in or security clearance.
   */
  setup_time?: number;
  /**
   * Cumulative distance from start to this activity in meters.
   */
  distance?: number;
  /**
   * Cumulative driving time from start to this activity in seconds.
   */
  driving_time?: number;
  /**
   * Array with size/capacity dimensions before this activity
   */
  load_before?: Array<number>;
  /**
   * Array with size/capacity dimensions after this activity
   */
  load_after?: Array<number>;
  /**
   * Priority copied from the underlying service or shipment (1 = highest, 10 = lowest, default 2). Omitted for `start`/`end` activities.
   */
  priority?: number;
  /**
   * Group identifier copied from the underlying activity (pickup or delivery side for shipments). Omitted for `start`/`end` activities and when the request did not specify a group.
   */
  group?: string;
  /**
   * The feasible departure window for the start activity. Only present on start activities when the vehicle has latest_start set. Shows the earliest and latest feasible departure times. (beta)
   */
  departure_window?: {
    /**
     * Earliest feasible departure time in seconds.
     */
    earliest?: number;
    /**
     * Latest feasible departure time in seconds.
     */
    latest?: number;
  };
  time_window?: TimeWindow & unknown;
};

export type GetRouteData = {
  body?: never;
  path?: never;
  query: {
    profile?: VehicleProfileId;
    /**
     * The points for which the route should be calculated. Format: `latitude,longitude`. Specify at least an origin and a destination. Via points are possible.
     * The maximum number depends on your plan.
     *
     */
    point: Array<string>;
    /**
     * The `point_hint` is typically a road name to which the associated `point` parameter should be snapped to. Specify no `point_hint` parameter or the same number as you have `point` parameters.
     *
     */
    point_hint?: Array<string>;
    /**
     * Optional parameter. 'Snapping' is the process of finding the closest road location for GPS coordinates provided in the `point` parameter. The `snap_prevention` parameter allows you to prevent snapping to specific types of roads. For example, if `snap_prevention` is set to bridge, the routing engine will avoid snapping to a bridge, even if it is the closest road for the given `point`. Current supported values: `motorway`, `trunk`, `ferry`, `tunnel`, `bridge` and `ford`. Multiple values are specified like `snap_prevention=ferry&snap_prevention=motorway`. Note that once snapped the routing algorithm can still route over bridges (or the other values). To avoid this you need to use the `custom_model`.
     *
     */
    snap_prevention?: Array<string>;
    /**
     * Optional parameter. It specifies on which side a point should be relative to the driver when she leaves/arrives at a start/target/via point. You need to specify this parameter for either none or all points. Only supported for motor vehicles and OpenStreetMap.
     *
     */
    curbside?: Array<"any" | "right" | "left">;
    /**
     * The locale of the resulting turn instructions. E.g. `pt_PT` for Portuguese or `de` for German.
     *
     */
    locale?: string;
    /**
     * If `true`, a third coordinate, the altitude, is included with all positions in the response.
     * This changes the format of the `points` and `snapped_waypoints` fields of the response, in both their
     * encodings. Unless you switch off the `points_encoded` parameter, you need special code on the
     * client side that can handle three-dimensional coordinates.
     *
     */
    elevation?: boolean;
    /**
     * Optional parameter. The following path details are available: `street_name`, `street_ref`, `street_destination`, `leg_time`, `leg_distance`, `roundabout`, `country`, `time`, `distance`, `max_speed`,
     * `max_weight`, `max_width`, `toll`, `road_class`, `road_class_link`, `road_access`, `road_environment`, `hazmat`, `hazmat_tunnel`, `hazmat_water`,
     * `lanes`, `surface`, `smoothness`, `hike_rating`, `mtb_rating`, `foot_network`, `bike_network`. Read more about the usage of path details [here](https://discuss.graphhopper.com/t/2539).
     *
     */
    details?: Array<string>;
    /**
     * Normally, the calculated route will visit the points in the order you specified them.
     * If you have more than two points, you can set this parameter to `"true"` and the points may be re-ordered to minimize the total travel time.
     * Keep in mind that the limits on the number of locations of the Route Optimization API applies, and the request costs more credits.
     *
     */
    optimize?: string;
    /**
     * If instructions should be calculated and returned
     *
     */
    instructions?: boolean;
    /**
     * If the points for the route should be calculated at all.
     *
     */
    calc_points?: boolean;
    /**
     * If `true`, the output will be formatted.
     *
     */
    debug?: boolean;
    /**
     * Allows changing the encoding of location data in the response. The default is polyline encoding, which is compact
     * but requires special client code to unpack. (We provide it in our JavaScript client library!)
     * Set this parameter to `false` to switch the encoding to simple coordinate pairs like `[lon,lat]`, or `[lon,lat,elevation]`.
     * See the description of the response format for more information.
     *
     */
    points_encoded?: boolean;
    /**
     * Use `true` to enable the flexible mode to use the `custom_model` or any of the following options below.
     *
     */
    "ch.disable"?: boolean;
    /**
     * Favour a heading direction for a certain point. Specify either one heading for the start point or as many as there are points.
     * In this case headings are associated by their order to the specific points. Headings are given as north based clockwise angle between 0 and 360 degree.
     * This parameter also influences the tour generated with `algorithm=round_trip` and forces the initial direction.  Requires `ch.disable=true`.
     *
     */
    heading?: Array<number>;
    /**
     * Time penalty in seconds for not obeying a specified heading. Requires `ch.disable=true`.
     *
     */
    heading_penalty?: number;
    /**
     * If `true`, u-turns are avoided at via-points with regard to the `heading_penalty`. Requires `ch.disable=true`.
     *
     */
    pass_through?: boolean;
    /**
     * Rather than looking for the shortest or fastest path, this parameter lets you solve two different problems related to routing:
     * With `alternative_route`, we give you not one but several routes that are close to optimal, but
     * not too similar to each other.
     * With `round_trip`, the route will get you back to where you started. This is meant for fun (think of
     * a bike trip), so we will add some randomness.
     * You can control both of these features with additional parameters, see below.
     *
     */
    algorithm?: "round_trip" | "alternative_route";
    /**
     * If `algorithm=round_trip`, this parameter configures approximative length of the resulting round trip.
     *
     */
    "round_trip.distance"?: number;
    /**
     * If `algorithm=round_trip`, this sets the random seed. Change this to get a different tour for each value.
     *
     */
    "round_trip.seed"?: number;
    /**
     * If `algorithm=alternative_route`, this parameter sets the number of maximum paths which should be calculated. Increasing can lead to worse alternatives.
     *
     */
    "alternative_route.max_paths"?: number;
    /**
     * If `algorithm=alternative_route`, this parameter sets the factor by which the alternatives routes can be longer than the optimal route. Increasing can lead to worse alternatives.
     *
     */
    "alternative_route.max_weight_factor"?: number;
    /**
     * If `algorithm=alternative_route`, this parameter specifies how similar an alternative route can be to the optimal route. Increasing can lead to worse alternatives.
     *
     */
    "alternative_route.max_share_factor"?: number;
  };
  url: "/route";
};

export type GetRouteErrors = {
  /**
   * Your request is not valid. For example, you specified too few or too many points.
   */
  400: GhError;
  /**
   * Authentication necessary
   */
  401: GhError;
  /**
   * API limit reached.
   */
  429: GhError;
  /**
   * Internal server error. We get notified automatically and fix this asap.
   */
  500: GhError;
};

export type GetRouteError = GetRouteErrors[keyof GetRouteErrors];

export type GetRouteResponses = {
  /**
   * Routing Result
   */
  200: RouteResponse;
};

export type GetRouteResponse = GetRouteResponses[keyof GetRouteResponses];

export type PostRouteData = {
  body?: RouteRequest;
  path?: never;
  query?: never;
  url: "/route";
};

export type PostRouteErrors = {
  /**
   * Your request is not valid. For example, you specified too few or too many points.
   */
  400: GhError;
  /**
   * Authentication necessary
   */
  401: GhError;
  /**
   * API limit reached.
   */
  429: GhError;
  /**
   * Internal server error. We get notified automatically and fix this asap.
   */
  500: GhError;
};

export type PostRouteError = PostRouteErrors[keyof PostRouteErrors];

export type PostRouteResponses = {
  /**
   * Routing Result
   */
  200: RouteResponse;
};

export type PostRouteResponse = PostRouteResponses[keyof PostRouteResponses];

export type GetIsochroneData = {
  body?: never;
  path?: never;
  query: {
    /**
     * Specify the start coordinate
     */
    point: string;
    /**
     * Specify which time the vehicle should travel. In seconds.
     */
    time_limit?: number;
    /**
     * Specify which distance the vehicle should travel. In meters.
     */
    distance_limit?: number;
    profile?: VehicleProfileId;
    /**
     * Number by which to divide the given `time_limit` to create `buckets` nested isochrones of time intervals `time_limit-n*time_limit/buckets`. Applies analogously to `distance_limit`.
     */
    buckets?: number;
    /**
     * If `false` the flow goes from point to the polygon, if `true` the flow goes from the polygon "inside" to the point.
     * Example use case for `false`&#58; *How many potential customer can be reached within 30min travel time from your store* vs. `true`&#58; *How many customers can reach your store within 30min travel time.*
     *
     */
    reverse_flow?: boolean;
  };
  url: "/isochrone";
};

export type GetIsochroneResponses = {
  /**
   * Isochrone Result
   */
  200: IsochroneResponse;
};

export type GetIsochroneResponse =
  GetIsochroneResponses[keyof GetIsochroneResponses];

export type GetMatrixData = {
  body?: never;
  path?: never;
  query?: {
    profile?: VehicleProfileId;
    /**
     * Specify multiple points in `latitude,longitude` for which the weight-, route-, time- or distance-matrix should be calculated. In this case the starts are identical to the destinations. If there are N points, then NxN entries will be calculated. The order of the point parameter is important. Specify at least three points. Cannot be used together with from_point or to_point.
     */
    point?: Array<string>;
    /**
     * The origin points for the routes in `latitude,longitude`. E.g. if you want to calculate the three routes A-&gt;1, A-&gt;2, A-&gt;3 then you have one from_point parameter and three to_point parameters.
     */
    from_point?: Array<string>;
    /**
     * The destination points for the routes in `latitude,longitude`.
     */
    to_point?: Array<string>;
    /**
     * Optional parameter. Specifies a hint for each `point` parameter to prefer a certain street for the closest location lookup. E.g. if there is an address or house with two or more neighboring streets you can control for which street the closest location is looked up.
     */
    point_hint?: Array<string>;
    /**
     * For the from_point parameter. See point_hint
     */
    from_point_hint?: Array<string>;
    /**
     * For the to_point parameter. See point_hint
     */
    to_point_hint?: Array<string>;
    /**
     * Optional parameter. 'Snapping' is the process of finding the closest road location for GPS coordinates provided in the `point` parameter. The `snap_prevention` parameter allows you to prevent snapping to specific types of roads. For example, if `snap_prevention` is set to bridge, the routing engine will avoid snapping to a bridge, even if it is the closest road for the given `point`. Current supported values: `motorway`, `trunk`, `ferry`, `tunnel`, `bridge` and `ford`. Multiple values are specified like `snap_prevention=ferry&snap_prevention=motorway`. Note that once snapped the routing algorithm can still route over bridges (or the other values). To avoid this you need to use the `custom_model`.
     *
     */
    snap_prevention?: Array<string>;
    /**
     * Optional parameter. It specifies on which side a point should be relative to the driver when she leaves/arrives at a start/target/via point. You need to specify this parameter for either none or all points. Only supported for motor vehicles and OpenStreetMap.
     */
    curbside?: Array<"any" | "right" | "left">;
    /**
     * Curbside setting for the from_point parameter. See curbside.
     */
    from_curbside?: Array<"any" | "right" | "left">;
    /**
     * Curbside setting for the to_point parameter. See curbside.
     */
    to_curbside?: Array<"any" | "right" | "left">;
    /**
     * Specifies which arrays should be included in the response. Specify one or more of the following options 'weights', 'times', 'distances'. To specify more than one array use e.g. out_array=times&out_array=distances. The units of the entries of distances are meters, of times are seconds and of weights is arbitrary and it can differ for different vehicles or versions of this API.
     */
    out_array?: Array<string>;
    /**
     * Specifies whether or not the matrix calculation should return with an error as soon as possible in case some points cannot be found or some points are not connected. If set to `false` the time/weight/distance matrix will be calculated for all valid points and contain the `null` value for all entries that could not be calculated. The `hint` field of the response will also contain additional information about what went wrong (see its documentation).
     */
    fail_fast?: boolean;
  };
  url: "/matrix";
};

export type GetMatrixResponses = {
  /**
   * Matrix API response
   */
  200: MatrixResponse;
};

export type GetMatrixResponse = GetMatrixResponses[keyof GetMatrixResponses];

export type PostMatrixData = {
  body?: MatrixRequest | SymmetricalMatrixRequest;
  path?: never;
  query?: never;
  url: "/matrix";
};

export type PostMatrixResponses = {
  /**
   * Matrix API response
   */
  200: MatrixResponse;
};

export type PostMatrixResponse = PostMatrixResponses[keyof PostMatrixResponses];

export type CalculateMatrixData = {
  body?: MatrixRequest | SymmetricalMatrixRequest;
  path?: never;
  query?: never;
  url: "/matrix/calculate";
};

export type CalculateMatrixResponses = {
  /**
   * A jobId you can use to retrieve your solution from the server.
   */
  200: JobId;
};

export type CalculateMatrixResponse =
  CalculateMatrixResponses[keyof CalculateMatrixResponses];

export type GetMatrixSolutionData = {
  body?: never;
  path: {
    /**
     * The jobId you received when you submitted the job.
     */
    jobId: string;
  };
  query?: never;
  url: "/matrix/solution/{jobId}";
};

export type GetMatrixSolutionResponses = {
  /**
   * A response containing the matrix
   */
  200: {
    solution?: MatrixResponse;
    status?: "processing" | "finished";
  };
};

export type GetMatrixSolutionResponse =
  GetMatrixSolutionResponses[keyof GetMatrixSolutionResponses];

export type PostGpxData = {
  /**
   * A GPX document containing the map-matching input.
   */
  body: unknown;
  path?: never;
  query?: {
    /**
     * Specify the precision of a point, in meter
     */
    gps_accuracy?: number;
    profile?: VehicleProfileId;
    /**
     * The locale of the resulting turn instructions. E.g. `pt_PT` for Portuguese or `de` for German.
     *
     */
    locale?: string;
    /**
     * If `true`, a third coordinate, the altitude, is included with all positions in the response.
     * This changes the format of the `points` and `snapped_waypoints` fields of the response, in both their
     * encodings. Unless you switch off the `points_encoded` parameter, you need special code on the
     * client side that can handle three-dimensional coordinates.
     *
     */
    elevation?: boolean;
    /**
     * Optional parameter. The following path details are available: `street_name`, `street_ref`, `street_destination`, `leg_time`, `leg_distance`, `roundabout`, `country`, `time`, `distance`, `max_speed`,
     * `max_weight`, `max_width`, `toll`, `road_class`, `road_class_link`, `road_access`, `road_environment`, `hazmat`, `hazmat_tunnel`, `hazmat_water`,
     * `lanes`, `surface`, `smoothness`, `hike_rating`, `mtb_rating`, `foot_network`, `bike_network`. Read more about the usage of path details [here](https://discuss.graphhopper.com/t/2539).
     *
     */
    details?: Array<string>;
    /**
     * If instructions should be calculated and returned
     *
     */
    instructions?: boolean;
    /**
     * If the points for the route should be calculated at all.
     *
     */
    calc_points?: boolean;
    /**
     * Allows changing the encoding of location data in the response. The default is polyline encoding, which is compact
     * but requires special client code to unpack. (We provide it in our JavaScript client library!)
     * Set this parameter to `false` to switch the encoding to simple coordinate pairs like `[lon,lat]`, or `[lon,lat,elevation]`.
     * See the description of the response format for more information.
     *
     */
    points_encoded?: boolean;
  };
  url: "/match";
};

export type PostGpxResponses = {
  /**
   * Map matching result
   */
  200: RouteResponse;
};

export type PostGpxResponse = PostGpxResponses[keyof PostGpxResponses];

export type GetGeocodeData = {
  body?: never;
  path?: never;
  query?: {
    /**
     * A textual description of the address you are looking for. Required for forward geocoding. Note that the `default` geocoding provider does prefix searches preferable for "autocomplete" use cases, but
     * may lead to sub-optimal results if used without user interaction. See e.g. `provider=nominatim` as an appropriate alternative for less interactive use cases.
     *
     */
    q?: string;
    /**
     * Display the search results for the specified locale. For the default provider French (fr), English (en) and German (de) are supported. Otherwise leave the locale empty.
     */
    locale?: string;
    /**
     * Specify the maximum number of results to return
     */
    limit?: number;
    /**
     * It is `required` to be `true` if you want to do a reverse geocoding request. If it is `true`, `point` must be defined as well, and `q` must not be used.
     */
    reverse?: boolean;
    /**
     * If `true`, the output will be formatted.
     */
    debug?: boolean;
    /**
     * _Forward geocoding_: The location bias in the format 'latitude,longitude' e.g. point=45.93272,11.58803. _Reverse geocoding_: The location to find amenities, cities.
     */
    point?: string;
    /**
     * The provider parameter is currently under development and can fall back to `default` at any time.
     * The intend is to provide alternatives to our default geocoder. Each provider has its own strenghts and might fit better for certain scenarios, so it's worth to compare the different providers.
     * To try it append the `provider`parameter to the URL like `&provider=nominatim`,
     * the result structure should be identical in all cases - if not, please report this back to us.
     * Keep in mind that some providers do not support certain parameters or don't return some fields, for example `osm_id` and `osm_type` are not supported by every geocoding provider.
     * If you would like to use additional parameters of one of the providers, but it's not available for the GraphHopper Geocoding API, yet? Please contact us.
     *
     * The credit costs can be different for all providers - see [here](https://support.graphhopper.com/support/solutions/articles/44000718211-what-is-one-credit-) for more information about it.
     *
     * Currently, only the default provider and gisgraphy support autocompletion of partial search strings.
     *
     * All providers support normal "forward" geocoding and reverse geocoding via `reverse=true`.
     *
     * #### Default (`provider=default`)
     *
     * This provider returns results of our internal geocoding engine. It is best suited for use cases with user interaction as it does prefix searches required for an "autocomplete" use case
     * (a user types an address into a search field).
     *
     * In addition to the above documented parameters the following parameters are possible:
     *
     * * `osm_tag` - you can filter `key:value` or exclude places with certain OpenStreetMap tags `!key:value`. E.g. `osm_tag=tourism:museum` or just the key `osm_tag=tourism`. To exclude multiple tags you add multiple `osm_tag` parameters.
     * * `location_bias_scale` - describes how much the prominence of a result should still be taken into account. Sensible values go from 0.0 (ignore prominence almost completely) to 1.0 (prominence has approximately the same influence as the location). The default is 0.2.
     * * `zoom` - describes the radius around the center to focus on. This is a number that should correspond roughly to the map zoom parameter of a corresponding map. The default is zoom=16.
     * * `bbox` - the expected format is `minLon,minLat,maxLon,maxLat`. This requires reverse=false.
     * * `radius` - the search radius in km for the reverse search. This requires reverse=true.
     *
     * #### Nominatim (`provider=nominatim`)
     *
     * The GraphHopper Directions API uses a commercially hosted Nominatim geocoder (hosted by [OpenCageData](https://opencagedata.com/)). It is best suited for use cases without or less user interaction
     * like batch processing or detailed location data retrieval. It is not suited for "autocomplete".
     *
     * In addition to the above documented parameters we currently support the following parameters:
     *
     * * countrycode - The country code is a two letter code as defined by the ISO 3166-1 Alpha 2 standard. E.g. gb for the United Kingdom, fr for France, us for United States.
     * * bounds - the expected format is `minLon,minLat,maxLon,maxLat`
     *
     * #### Gisgraphy (`provider=gisgraphy`)
     *
     * This provider returns results from the Gisgraphy geocoder which you can try [here](https://services.gisgraphy.com/static/leaflet/index.html).
     *
     * **Limitations:** The `locale` parameter is not supported. Gisgraphy does not return OSM tags or an extent.
     *
     * Gisgraphy has a special autocomplete API, which you can use by adding `autocomplete=true` (does not work with `reverse=true`). The autocomplete API is optimized on predicting text input, but returns less information.
     *
     * In addition to the above documented parameters Gisgraphy allows to use the following parameters, which can be used as documented [here](https://www.gisgraphy.com/documentation/user-guide.php#geocodingservice):
     *
     * * `radius` - radius in meters
     * * `country` - restrict search for the specified country. The value must be the ISO 3166 Alpha 2 code of the country.
     *
     * #### OpenCage Data (`provider=opencagedata`)
     *
     * This provider returns results from the OpenCageData geocoder which you can try [here](https://geocoder.opencagedata.com/demo).
     * The difference to the `nominatim` provider is that [other geocoders](https://opencagedata.com/credits) might be used under the hood.
     *
     * In addition to the above documented parameters OpenCage Data allows to use the following parameters, which can be used as documented [here](https://geocoder.opencagedata.com/api#forward-opt):
     *
     * * countrycode - The country code is a two letter code as defined by the ISO 3166-1 Alpha 2 standard. E.g. gb for the United Kingdom, fr for France, us for United States.
     * * bounds - the expected format is `minLon,minLat,maxLon,maxLat`
     *
     */
    provider?: string;
  };
  url: "/geocode";
};

export type GetGeocodeResponses = {
  /**
   * An array found locations
   */
  200: GeocodingResponse;
};

export type GetGeocodeResponse = GetGeocodeResponses[keyof GetGeocodeResponses];

export type SolveVrpData = {
  /**
   * The request that contains the vehicle routing problem to be solved.
   */
  body: Request;
  path?: never;
  query?: never;
  url: "/vrp";
};

export type SolveVrpErrors = {
  /**
   * Error occurred when reading the request. Request is invalid.
   */
  400: BadRequest;
  /**
   * Error occurred on server side.
   */
  500: InternalErrorMessage;
};

export type SolveVrpError = SolveVrpErrors[keyof SolveVrpErrors];

export type SolveVrpResponses = {
  /**
   * A response containing the solution
   */
  200: Response;
};

export type SolveVrpResponse = SolveVrpResponses[keyof SolveVrpResponses];

export type AsyncVrpData = {
  /**
   * The request that contains the problem to be solved.
   */
  body: Request;
  path?: never;
  query?: never;
  url: "/vrp/optimize";
};

export type AsyncVrpErrors = {
  /**
   * Error occurred when reading client request. Request is invalid.
   */
  400: BadRequest;
  /**
   * Error occurred on server side.
   */
  500: InternalErrorMessage;
};

export type AsyncVrpError = AsyncVrpErrors[keyof AsyncVrpErrors];

export type AsyncVrpResponses = {
  /**
   * A jobId you can use to retrieve your solution from the server - see solution endpoint.
   */
  200: JobId;
};

export type AsyncVrpResponse = AsyncVrpResponses[keyof AsyncVrpResponses];

export type GetSolutionData = {
  body?: never;
  path: {
    /**
     * Request solution with jobId
     */
    jobId: string;
  };
  query?: never;
  url: "/vrp/solution/{jobId}";
};

export type GetSolutionErrors = {
  /**
   * Error occurred on client side such as invalid input.
   */
  400: BadRequest;
  /**
   * Requested solution could not be found.
   */
  404: {
    /**
     * Error message
     */
    message?: string;
    /**
     * status
     */
    status?: string;
  };
  /**
   * Error occurred on server side.
   */
  500: unknown;
};

export type GetSolutionError = GetSolutionErrors[keyof GetSolutionErrors];

export type GetSolutionResponses = {
  /**
   * A response containing the solution
   */
  200: Response;
};

export type GetSolutionResponse =
  GetSolutionResponses[keyof GetSolutionResponses];

export type SolveClusteringProblemData = {
  /**
   * Request object that contains the problem to be solved
   */
  body: ClusterRequest;
  path?: never;
  query?: never;
  url: "/cluster";
};

export type SolveClusteringProblemErrors = {
  /**
   * Error occurred when reading the request. Request is invalid.
   */
  400: BadRequest;
  /**
   * Error occurred on server side.
   */
  500: InternalErrorMessage;
};

export type SolveClusteringProblemError =
  SolveClusteringProblemErrors[keyof SolveClusteringProblemErrors];

export type SolveClusteringProblemResponses = {
  /**
   * A response containing the solution
   */
  200: ClusterResponse;
};

export type SolveClusteringProblemResponse =
  SolveClusteringProblemResponses[keyof SolveClusteringProblemResponses];

export type AsyncClusteringProblemData = {
  /**
   * Request object that contains the problem to be solved
   */
  body: ClusterRequest;
  path?: never;
  query?: never;
  url: "/cluster/calculate";
};

export type AsyncClusteringProblemErrors = {
  /**
   * Error occurred when reading client request. Request is invalid.
   */
  400: BadRequest;
  /**
   * Error occurred on server side.
   */
  500: InternalErrorMessage;
};

export type AsyncClusteringProblemError =
  AsyncClusteringProblemErrors[keyof AsyncClusteringProblemErrors];

export type AsyncClusteringProblemResponses = {
  /**
   * A jobId you can use to retrieve your solution from the server - see solution endpoint.
   */
  200: JobId;
};

export type AsyncClusteringProblemResponse =
  AsyncClusteringProblemResponses[keyof AsyncClusteringProblemResponses];

export type GetClusterSolutionData = {
  body?: never;
  path: {
    /**
     * Request solution with jobId
     */
    jobId: string;
  };
  query?: never;
  url: "/cluster/solution/{jobId}";
};

export type GetClusterSolutionErrors = {
  /**
   * Error occurred on client side such as invalid input.
   */
  400: BadRequest;
  /**
   * Requested solution could not be found.
   */
  404: {
    /**
     * Error message
     */
    message?: string;
    /**
     * status
     */
    status?: string;
  };
  /**
   * Error occurred on server side.
   */
  500: unknown;
};

export type GetClusterSolutionError =
  GetClusterSolutionErrors[keyof GetClusterSolutionErrors];

export type GetClusterSolutionResponses = {
  /**
   * A response containing the solution
   */
  200: ClusterResponse;
};

export type GetClusterSolutionResponse =
  GetClusterSolutionResponses[keyof GetClusterSolutionResponses];

export type GetProfileData = {
  body?: never;
  path?: never;
  query?: never;
  url: "/profiles";
};

export type GetProfileResponses = {
  /**
   * Your profiles
   */
  200: ProfileGetResponse;
};

export type GetProfileResponse = GetProfileResponses[keyof GetProfileResponses];

export type PostProfileData = {
  body: ProfileRequest;
  path?: never;
  query?: never;
  url: "/profiles";
};

export type PostProfileResponses = {
  /**
   * Profile created
   */
  201: ProfileResponse;
};

export type PostProfileResponse =
  PostProfileResponses[keyof PostProfileResponses];

export type CalculateProfileData = {
  body: ProfileRequest;
  path?: never;
  query?: never;
  url: "/profiles/calculate";
};

export type CalculateProfileResponses = {
  /**
   * A jobId you can use to retrieve the calculated profile.
   */
  200: JobId;
};

export type CalculateProfileResponse =
  CalculateProfileResponses[keyof CalculateProfileResponses];

export type GetProfileSolutionData = {
  body?: never;
  path: {
    /**
     * The jobId you received when you submitted the job.
     */
    jobId: string;
  };
  query?: never;
  url: "/profiles/solution/{jobId}";
};

export type GetProfileSolutionResponses = {
  /**
   * A response containing the custom profile
   */
  200: {
    solution?: ProfileResponse;
    status?: "processing" | "finished";
  };
};

export type GetProfileSolutionResponse =
  GetProfileSolutionResponses[keyof GetProfileSolutionResponses];

export type DeleteProfileData = {
  body?: never;
  path: {
    /**
     * The profile to delete
     */
    profileId: string;
  };
  query?: never;
  url: "/profiles/{profileId}";
};

export type DeleteProfileResponses = {
  /**
   * A response containing the status
   */
  200: {
    status?: "success";
  };
};

export type DeleteProfileResponse =
  DeleteProfileResponses[keyof DeleteProfileResponses];
